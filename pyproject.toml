[project]
name = "openai-register"
version = "0.1.0"
description = "OpenAI 自動註冊工具"
requires-python = ">=3.12"
dependencies = [
    "curl-cffi>=0.14.0",
    "fastapi>=0.135.3",
    "httpx>=0.28.1",
    "playwright>=1.58.0",
    "uvicorn>=0.43.0",
]

[dependency-groups]
dev = []
